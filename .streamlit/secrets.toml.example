# Streamlit Secrets Configuration Template
#
# Instructions:
# 1. Copy this file to 'secrets.toml' in the same directory
#    cp .streamlit/secrets.toml.example .streamlit/secrets.toml
#
# 2. Replace placeholder values with your actual credentials
#
# 3. DO NOT commit secrets.toml to git (it's in .gitignore)
#
# For Streamlit Cloud deployment:
#   Add these secrets in the deployment settings under "Secrets" section

# ============================================================================
# REQUIRED: Google Gemini API (for both Gemini and RAG agents)
# ============================================================================
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY = "your_gemini_api_key_here"

# Optional: Override default model (default: gemini-2.5-flash)
GEMINI_MODEL = "gemini-2.5-flash"

# ============================================================================
# OPTIONAL: Qdrant Cloud (for Phase 2 RAG Agent with advanced features)
# ============================================================================
# Without these, RAG agent will work with sample knowledge base only
# Get free account at: https://cloud.qdrant.io/

# Qdrant Cluster URL (format: https://xxx-xxx.region.cloud.qdrant.io)
QDRANT_URL = "https://your-cluster.aws.cloud.qdrant.io"

# Qdrant API Key (format: qc_xxxxxxxxxxxxxxxxx)
QDRANT_API_KEY = "your_qdrant_api_key_here"

# ============================================================================
# Notes:
# ============================================================================
# - Gemini Agent: Only needs GEMINI_API_KEY
# - RAG Agent (basic): Works with GEMINI_API_KEY + sample knowledge
# - RAG Agent (full): Needs all three keys for Phase 2 features
#   (hybrid search, re-ranking, 6,328 medical documents)
#
# See SETUP.md for detailed configuration instructions
